import React, { FunctionComponent } from 'react'
import * as Styled from './Landing.styled'
type Props = {
  //
}
export const Landing: FunctionComponent<Props> = () => {
  return <Styled.wrapper>Homepage</Styled.wrapper>
}
